package curd.operations;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

@SpringBootApplication
public class OperationsApplication {

	public static void main(String[] args) {
		SpringApplication.run(OperationsApplication.class, args);
	}

}
// This is the main application class for the CRUD operations application. It uses the @SpringBootApplication annotation to enable auto-configuration and component scanning. The main method starts the Spring Boot application by calling SpringApplication.run() with the OperationsApplication class and command-line arguments.
